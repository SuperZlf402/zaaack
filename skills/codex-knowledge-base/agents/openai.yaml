interface:
  display_name: "Codex Knowledge Base"
  short_description: "Build a safe Codex memory and chat knowledge base."
  default_prompt: "Use $codex-knowledge-base to collect Codex memories, habits, and chat history from this computer, then stage the sanitized export under a device-specific GitHub folder."
policy:
  allow_implicit_invocation: true
